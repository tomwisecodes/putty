import * as React from 'react';
import { transformContext, IkonateContext } from '../context';
export const CheckboxIntermediate = React.forwardRef((props, svgRef) => {
    const value = React.useContext(IkonateContext);
    const extraProps = transformContext(value);
    return React.createElement("svg", Object.assign({ width: "1em", height: "1em", viewBox: "0 0 24 24", "aria-labelledby": "checkboxIntermediateIconTitle", strokeLinejoin: "miter", strokeLinecap: "square", strokeWidth: "2" }, extraProps, { stroke: "currentColor", fill: "none", ref: svgRef }, props),
        React.createElement("title", { id: "checkboxIntermediateIconTitle" }, "Checkbox in intermediate state"),
        React.createElement("rect", { x: 21, y: 3, width: 18, height: 18, rx: 1, transform: "rotate(90 21 3)" }),
        React.createElement("path", { d: "M16 12H8" }));
});
